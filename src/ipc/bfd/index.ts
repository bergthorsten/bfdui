import {
  getConfig,
  getDevDeployments,
  getSprintTickets,
  getTicketDevelopment,
  saveConfig,
  searchTickets,
  testConnection,
} from "./handlers";

export const bfd = {
  getConfig,
  getDevDeployments,
  getSprintTickets,
  getTicketDevelopment,
  saveConfig,
  searchTickets,
  testConnection,
};
