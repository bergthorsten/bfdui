import {
  checkEnvironment,
  getConfig,
  getDevDeployments,
  getSprintTickets,
  getTicketDevelopment,
  saveConfig,
  searchTickets,
  testConnection,
} from "./handlers";

export const bfd = {
  checkEnvironment,
  getConfig,
  getDevDeployments,
  getSprintTickets,
  getTicketDevelopment,
  saveConfig,
  searchTickets,
  testConnection,
};
